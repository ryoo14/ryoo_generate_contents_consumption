export type Anime = {
  date: string;
  title: string;
  episode: number;
  url: string;
};

export type Book = {
  title: string;
  readDate: string;
  url: string;
};

export type Movie = {
  title: string;
  url: string;
};
